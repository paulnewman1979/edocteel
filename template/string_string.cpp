#include <iostream>
#include <vector>
#include <algorithm>
#include <stdio.h>

using namespace std;

class Solution {
public:
    string addBinary(string a, string b) {
        
    }
};

int main(int argc, char* argv[])
{
	string a;
	string b;

	getline(cin, a);
	getline(cin, b);
    Solution solution;
	cout << solution.addBinary(a, b) << endl;

    return 0;
}
